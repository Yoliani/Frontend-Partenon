/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['img.freepik.com', 'concepto.de', 'www.lancetalent.com'],
  },
};

module.exports = nextConfig;
